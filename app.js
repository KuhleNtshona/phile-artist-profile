const motionButton=document.querySelector('#motion');const reduced=window.matchMedia('(prefers-reduced-motion: reduce)');let paused=reduced.matches;function syncMotion(){document.documentElement.classList.toggle('motion-off',paused);motionButton.setAttribute('aria-pressed',String(paused));motionButton.textContent=paused?'Enable motion':'Pause motion';document.querySelectorAll('[data-depth],[data-tilt]').forEach(el=>el.style.transform='');}syncMotion();motionButton.addEventListener('click',()=>{paused=!paused;syncMotion()});reduced.addEventListener('change',e=>{paused=e.matches;syncMotion()});let frame=false;window.addEventListener('scroll',()=>{if(frame||paused)return;frame=true;requestAnimationFrame(()=>{const photo=document.querySelector('[data-depth]');if(scrollY<innerHeight*1.5)photo.style.transform=`translate3d(0,${Math.min(scrollY*.12,100)}px,0)`;frame=false})},{passive:true});document.querySelectorAll('[data-tilt]').forEach(el=>{el.addEventListener('pointermove',e=>{if(paused||e.pointerType!=='mouse')return;const r=el.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(1200px) rotateX(${-y*4}deg) rotateY(${x*4}deg)`});el.addEventListener('pointerleave',()=>el.style.transform='')});
document.querySelectorAll('.colour-toggle').forEach(button=>button.addEventListener('click',()=>{const enabled=button.getAttribute('aria-pressed')!=='true';button.setAttribute('aria-pressed',String(enabled));button.textContent=enabled?'Return to monochrome ↗':'Reveal colour ↗';button.closest('figure').classList.toggle('is-colour',enabled)}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){if(!paused)entry.target.classList.add('reveal');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.spread h2,.section-meta').forEach(el=>observer.observe(el));
const header=document.querySelector('header');const navLinks=[...document.querySelectorAll('nav a')];const navTargets=navLinks.map(link=>document.querySelector(link.hash));new ResizeObserver(()=>document.documentElement.style.setProperty('--nav-height',`${header.offsetHeight}px`)).observe(header);
function updateNavigation(){const boundary=header.offsetHeight+80;let current=null;for(const target of navTargets){if(target.getBoundingClientRect().top<=boundary)current=target.id}navLinks.forEach(link=>{if(link.hash===`#${current}`)link.setAttribute('aria-current','location');else link.removeAttribute('aria-current')})}let navFrame=false;window.addEventListener('scroll',()=>{if(navFrame)return;navFrame=true;requestAnimationFrame(()=>{updateNavigation();navFrame=false})},{passive:true});window.addEventListener('hashchange',updateNavigation);window.addEventListener('load',updateNavigation);updateNavigation();
document.querySelectorAll('a[href^="#"]').forEach(link=>link.addEventListener('click',event=>{if(event.ctrlKey||event.metaKey||event.shiftKey||event.altKey)return;const target=document.querySelector(link.hash);if(!target)return;target.setAttribute('tabindex','-1');target.focus({preventScroll:true})}));
